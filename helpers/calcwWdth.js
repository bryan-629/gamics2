import { Dimensions, StyleSheet, Text, View, SafeAreaView,TouchableOpacity} from 'react-native';
export const windowWidth = Dimensions.get('window').width;
export const anchoToltaCols = Dimensions.get('window').width * 0.9;
export const windowWidthCol = Dimensions.get('window').width * 0.20;